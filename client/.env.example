# Optional: absolute API origin (no trailing slash). Example: https://api.example.com
# Leave empty in development so requests use /api and the Vite proxy reaches the backend.
VITE_API_URL=

# Dev only: where Vite proxies /api (default http://localhost:5000). Match server PORT.
VITE_PROXY_TARGET=http://localhost:5000
